# Offline-First Notes with Cloud Sync

A Flutter notes app that works fully offline and syncs automatically to Firebase Firestore when reconnected — built with Drift, documented conflict resolution, and a hand-written native Android platform bridge.

🚧 Work in progress — built stage by stage. See commit history for progress.